uuid: e3bdbebb-12d9-4a37-9d43-4f42ba7fed1b
langcode: en
status: true
dependencies:
  config:
    - node.type.internal_audit
    - node.type.planned_events
  module:
    - content_moderation
id: workflow_for_audit_planning
label: 'Workflow for Audit Planning'
type: content_moderation
type_settings:
  states:
    draft:
      label: Draft
      published: false
      default_revision: false
      weight: 0
    post_audit:
      published: true
      default_revision: true
      label: 'Post Audit'
      weight: 6
    pre_audit:
      published: true
      default_revision: true
      label: 'Pre Audit'
      weight: 2
    published:
      label: Published
      published: true
      default_revision: true
      weight: 1
    reschedule:
      published: true
      default_revision: true
      label: Reschedule
      weight: 4
    scheduled:
      published: true
      default_revision: true
      label: Scheduled
      weight: 5
  transitions:
    post_audit:
      label: 'Post Audit'
      from:
        - pre_audit
        - scheduled
      to: post_audit
      weight: 7
    pre_audit:
      label: 'Pre Audit'
      from:
        - draft
      to: pre_audit
      weight: 4
    reschedule:
      label: Reschedule
      from:
        - scheduled
      to: reschedule
      weight: 5
    scheduled:
      label: Scheduled
      from:
        - draft
        - post_audit
        - pre_audit
        - published
        - reschedule
        - scheduled
      to: scheduled
      weight: 6
  entity_types:
    node:
      - internal_audit
      - planned_events
  default_moderation_state: draft
